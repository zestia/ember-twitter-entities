{{#each this.parts as |part|}}
  {{~#if part.Component~}}
    {{~#let part.Component as |Entity|~}}
      <Entity @entity={{part.entity}} />
    {{~/let~}}
  {{else if part.string~}}
    {{part.string}}
  {{~/if}}
{{/each}}