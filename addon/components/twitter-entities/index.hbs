{{#each this.parts as |part|}}
  {{#if part.text~}}
    {{part.text}}
  {{~else~}}
    {{~#let (component part.Component) as |Part|~}}
      <Part @entity={{part.entity}} />
    {{~/let~}}
  {{~/if}}
{{/each}}