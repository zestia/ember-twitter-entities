{{#each this.parts as |part|}}
  {{~#if part.component~}}
    {{~#let (component part.component) as |Entity|~}}
      <Entity @entity={{part.entity}} />
    {{~/let~}}
  {{else if part.string~}}
    {{part.string}}
  {{~/if}}
{{/each}}